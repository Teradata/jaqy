.echo
-----

``.echo`` turns input echo on / off.

Syntax
~~~~~~

.. code-block:: text

	usage: .echo [on | off | auto]

The default is ``auto`` which has the following behavior.  If the input is
interactive, then echo is off.  Otherwise, it is on.

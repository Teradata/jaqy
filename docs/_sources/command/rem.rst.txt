.rem
----

``.rem`` (remark) does block comment.

Example
~~~~~~~

.. code-block:: sql

	.rem nothing gets executed
	SELECT 1234;
	SELECT 1234;
	SELECT 1234;
	.end rem

See Also
~~~~~~~~

* `Comments <../comment.html>`__

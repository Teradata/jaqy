Settings
========

`.set <command/set.html>`__ command is used to set or display settings.

There are two types of settings.

* Interpreter level is active for the interpreter.
* Session level setting is only active for a connected session.

.. toctree::
	:maxdepth: 0
	:glob:
	:hidden:

	setting/*
